If a username and a password is not given when we clone a repository 
from the remote server, after modifying a file, we can commit to the
local repository but cannot push the updated content to the remote server.
